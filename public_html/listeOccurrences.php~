<?php
error_reporting(E_ALL);

function listeOccurences($re, $chaine) {
  if (!(preg_match_all($re, $chaine, $res)) {
    return "<h1>Erreur: La chaine ne correspond pas au motif</h1";
  }
    $legend="$re $chaine";
    return array_to_table($res[0], $legend);
  }
    
?>
