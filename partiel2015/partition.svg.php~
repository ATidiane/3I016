<?php
error_reporting(E_ALL);

function partition2svg($partition, $hauteur='', $largeur='', $w='') {

  foreach ($partition as $k => $v) {
    foreach ($v as $k1 => $v1) {
      echo "$k1:$v1";
    }
  }
}

$t = 'include extration.sax.php';
partition2svg($t, 
?>
