<?php
error_reporting(E_ALL);
function get_mail_count($sock) {
    $res = commande($sock, "STAT");
    if ($res) {
        $tab = explode(" ", $res);
        foreach ($tab as $index => $val) {
           
        }
    }
}
?>